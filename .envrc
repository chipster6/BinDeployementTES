# Set agent context for policy enforcement
# Change this per terminal window to match your current agent role
export AGENT=external_api

# Uncomment and set the appropriate agent for your current work:
# export AGENT=architecture      # for contracts, docs, ADRs
# export AGENT=db_architect      # for schema.dev.sql, compose, seeds
# export AGENT=external_api      # for controllers, routes, middleware
# export AGENT=ingestion         # for ingestion controllers/services
# export AGENT=orders           # for orders controllers/services
# export AGENT=eventing_obs     # for eventing, infra, observability